{{-- @foreach ($produits as $produit)
<div class="product-card p-4 bg-white rounded shadow mb-4">
    <h3 class="text-green-700 font-bold">{{ $produit->name }}</h3>
    <p>{{ Str::limit($produit->description, 70) }}</p>
    <span>Price: {{ $produit->price }} MAD</span>
    <span>Category: {{ $produit->Category->name }}</span>
</div>
@endforeach --}}
